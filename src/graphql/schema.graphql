# source: http://localhost:3000/graphql/
# timestamp: Tue Mar 03 2020 17:09:10 GMT-0800 (Pacific Standard Time)

type Company {
    id: Int!
    name: String!
    logo: String!
    location: String!
    specialties: [Specialty!]!
}

type CompanyDB {
    id: Int!
    name: String!
    logo: String!
    location: String!
    specialties: [Int!]!
}

type PageMetadata {
    index: Int!
    size: Int!
    total: Int!
}

type PaginatedCompanies {
    results: [Company!]!
    page: PageMetadata!
}

type Query {
    specialties: [Specialty!]!
    companies(
        search: String
        specialties: [Int!]
        pageIndex: Int
        pageSize: Int
    ): PaginatedCompanies!
}

type Specialty {
    id: Int!
    name: String!
}
